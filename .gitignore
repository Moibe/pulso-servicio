node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# OS
.DS_Store
Thumbs.db

# SQLite (datos locales)
local.db
local.db-shm
local.db-wal

# Restos de la carpeta de imágenes del catálogo que se retiró (ya nada la escribe;
# en el droplet puede quedar con fotos viejas hasta que se borre a mano).
/uploads

# Env
.env
.env.*
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*
