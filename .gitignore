node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# OS
.DS_Store
Thumbs.db

# SQLite (datos locales)
local.db
local.db-shm
local.db-wal

# Archivos subidos (fotos de productos, etc.) — persisten en disco, no en git.
# Anclado a la raíz (/uploads) para no atrapar también src/routes/uploads/.
/uploads

# Env
.env
.env.*
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*
